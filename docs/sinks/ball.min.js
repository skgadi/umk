function init(){camera=new THREE.PerspectiveCamera(70,window.innerWidth/window.innerHeight,.01,10),camera.position.z=1,scene=new THREE.Scene,geometry=new THREE.BoxGeometry(.2,.2,.2),material=new THREE.MeshNormalMaterial,mesh=new THREE.Mesh(geometry,material),scene.add(mesh),renderer=new THREE.WebGLRenderer({antialias:!0}),renderer.setSize(window.innerWidth,window.innerHeight),document.body.appendChild(renderer.domElement)}function animate(){requestAnimationFrame(animate),mesh.rotation.x+=.01,mesh.rotation.y+=.02,renderer.render(scene,camera)}import*as THREE from"../dependencies/three/build/three.module.js";var camera,scene,renderer,geometry,material,mesh;init(),animate();